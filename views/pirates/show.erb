<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Pirate and his Ships</title>
  </head>
  <body>
    <h1>Pirate</h1>
    <div class="pirate">
      <h3>Name: <%= @pirate.name %></h3>
      <h4>Weight: <%= @pirate.weight %></h4>
      <h4>Height: <%= @pirate.height %></h4>
    </div>

    <h2>Ships Deatils</h2>
    <% @ships.each do |ship| %>
    <div class="ship">
      <p>Name: <%= ship.name %></p>
      <p>Type: <%= ship.type %></p>
      <p>Booty: <%= ship.booty %></p>
    </div>
    <% end %>
  </body>
</html>
