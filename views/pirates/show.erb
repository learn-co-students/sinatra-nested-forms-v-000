<!DOCTYPE html>
<html>
  <head>
    <title></title>
  </head>
  <body>
    <%= @pirate.name %>
    <%= @pirate.weight %>
    <%= @pirate.height %>
    <% @ships.each do |ship| %>
      <%= ship.name %>
      <%= ship.type %>
      <%= ship.booty %>
    <% end %>
  </body>
</html>
